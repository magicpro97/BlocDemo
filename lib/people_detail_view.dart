import 'package:flutter/material.dart';

class PeopleDetailView extends StatelessWidget {
  const PeopleDetailView({Key? key, required this.name}) : super(key: key);

  final String name;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
